import { DateGeneratorDirective } from './date-generator.directive';

describe('DateGeneratorDirective', () => {
  it('should create an instance', () => {
    const directive = new DateGeneratorDirective();
    expect(directive).toBeTruthy();
  });
});
