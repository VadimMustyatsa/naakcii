# Spring Boot
spring.jmx.default-domain=by.naakcii.api
server.servlet.context-path=/

# Database Connection Pool
spring.datasource.dbcp2.initial-size=2
spring.datasource.dbcp2.max-total=12

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/test?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.username=${POSTGRESQL_USER}
spring.datasource.password=${POSTGRESQL_PASSWORD}

# MySQL
#spring.datasource.url=jdbc:mysql://localhost:3306/test?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&&serverTimezone=Europe/Moscow
#spring.datasource.username=${MYSQL_USER}
#spring.datasource.password=${MYSQL_PASSWORD}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.type=trace
spring.jpa.properties.hibernate.show_sql=false
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=create
#spring.jpa.hibernate.naming.physical-strategy=naakcii.by.api.util.NamingStrategy

# Logging
spring.output.ansi.enabled=ALWAYS
logging.level.org.hibernate.type.descriptor.sql=DEBUG
logging.level.org.hibernate.SQL=TRACE

# Ftp chains data path: {data.path}/chain_synonym/{folder.name}
ftp.action.data.path=c:/data/
ftp.action.data.folder.name=action_data
ftp.action.log.path=c:/data/logs/

# Slack API
slack.bot.oauth.token=${SLACK_BOT_OAUTH_TOKEN}
slack.notification.channel=notifications
